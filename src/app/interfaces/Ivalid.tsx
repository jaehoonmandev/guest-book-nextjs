
export interface postValidInterface{
    title : boolean,
    contents : boolean,
    writer : boolean,
    permitCode: boolean,
    color: boolean,
}

export interface putValidInterface{
    title : boolean,
    contents : boolean,
    color: boolean,
}